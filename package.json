{
  "name": "planyourfit",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev -w server\" \"npm start -w client\"",
    "start": "npm run start -w server",
    "build": "npm run build -w client",
    "test": "npm run test -w server && npm run test -w client -- --watchAll=false",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
